<% content_for :mtitle, @sale.mtitle %>
<% content_for :mdescription, @sale.mdescription %>

<% content_for :title, @sale.name %>
<% content_for :type, 'discounts' %>

<div class="sale">
    <div class="row offer_bloks">
        <%= render @discounts %>
    </div>
</div>
<div class="paginator d-flex justify-content-center">
	<nav aria-label="Page navigation">
		<%= will_paginate @discounts, renderer: @custom_paginate_renderer %>
	</nav>
</div>