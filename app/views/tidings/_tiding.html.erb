    <tr>
      <td><%= tiding.name %></td>
      <td><%= tiding.parent.name if tiding.parent.present? %></td>
      <td><%= link_to 'Show', tiding_path(tiding) %></td> 
      <td><%= link_to 'Edit', edit_tiding_path(tiding) %></td> 
      <td><%= link_to 'Destroy', tiding, method: :delete, data: { confirm: 'Are you sure?' } %></td>
    </tr>