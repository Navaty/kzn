<% content_for :title, @posts.name %>
<% content_for :type, 'posts' %>

<h1><%=@tiding.name%></h1>
<div class="tiding">
    <div class="row offer_bloks">
        <%= render @posts %>
    </div>
</div>