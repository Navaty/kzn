<% content_for :mtitle, @tiding.mtitle %>
<% content_for :mdescription, @tiding.mdescription %>

<% content_for :title, @tiding.name %>
<% content_for :type, 'posts' %>

<div class="tiding">
    <div class="row offer_bloks">
        <%= render @posts %>
    </div>
</div>
<div class="paginator d-flex justify-content-center">
	<nav aria-label="Page navigation">
		<%= will_paginate @posts, renderer: @custom_paginate_renderer %>
	</nav>
</div>