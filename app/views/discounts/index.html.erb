			<%# render 'shared/catslider' %> 
			<%# render 'shared/catnav' %> 
			<%# render 'shared/src' %> 
<% content_for :type, 'discounts' %>

<% if user_signed_in? %>
<div class="buttons_panel">
  <%= link_to 'Новая скидка', new_admin_discount_path, class: 'btn btn-outline-primary btn-sm' %>
</div>		
<br>
<% end %>

<div class="sale" id="discounts">
 <%= render 'discounts' %>
</div>
