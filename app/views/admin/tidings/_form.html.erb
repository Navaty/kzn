<%= form_with(model: [:admin, @tiding], local: true) do |form| %>

<% if tiding.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(tiding.errors.count, "error") %> prohibited this post from being saved:</h2>

        <ul>
        <% tiding.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    <% end %>


  <div class="form-group">
    <%= form.label :name %><br>
    <%= form.text_field :name, class:"form-control"  %>
  </div>
  <div class="form-group">
    <%= form.label :Родитель %><br>
    <%= form.collection_select :parent_id, Tiding.order(:name), :id, :name, {include_blank: true }, class:"form-control"  %> <%# where("id != #{@sale.id}") %>
  </div>

    <div class="form-group">
      <%= form.label :meta, "Метатеги:" %><br>
    <div class="form-group">
      <%= form.label :mtitle, "Title" %><br>
      <%= form.text_field :mtitle, class:"form-control" %>
    </div>

      <div class="form-group">
      <%= form.label :mdescription, "Description" %><br>
      <%= form.text_field :mdescription, class:"form-control" %>
    </div>
  </div>
  

  <div class="form-group">
    <%= form.submit "Сохранить", class: "btn btn-primary" %>
    <%= link_to 'Назад', admin_tidings_path, class: "btn btn-secondary" %>

  </div>

<% end %>