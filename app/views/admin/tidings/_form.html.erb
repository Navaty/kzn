<%= form_with(model: [:admin, @tiding], local: true) do |form| %>
<% if tiding.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(tiding.errors.count, "error") %> prohibited this post from being saved:</h2>

        <ul>
        <% tiding.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    <% end %>

  <div>
    <%= form.label :name %><br>
    <%= form.text_field :name %>
  </div>
  <div>
    <%= form.label :Родитель %><br>
    <%= form.collection_select :parent_id, Tiding.order(:name), :id, :name, {include_blank: true } %> <%# where("id != #{@sale.id}") %>
  </div>
  

  <div>
    <%= form.submit %>
  </div>

<% end %>

<%= link_to 'Back', tidings_path %>